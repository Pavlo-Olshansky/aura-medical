cd frontend && npx lint-staged

# Lint and type-check staged Python files
STAGED_PY=$(git diff --cached --name-only --diff-filter=ACM -- 'backend/*.py')
if [ -n "$STAGED_PY" ]; then
  ruff check $STAGED_PY
  cd ../backend && mypy $STAGED_PY
fi
